services:
  db:
    image: postgres:alpine
    ports:
      - 5432:5432
    volumes:
      - .docker/db:/var/lib/postgresql
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=splitiv
